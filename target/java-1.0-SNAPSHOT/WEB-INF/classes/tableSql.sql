-- Create table
create table USER_ACCOUNT
(
/*  ID MEDIUMINT  NOT NULL AUTO_INCREMENT,
*/
  USER_NAME VARCHAR(30) not null,
  GENDER    VARCHAR(1) not null,
  PASSWORD  VARCHAR(30) not null,
  primary key (USER_NAME)
);

-- Create table
create table PRODUCT
(
  CODE  VARCHAR(20) not null,
  NAME  VARCHAR(128) not null,
  PRICE FLOAT not null,
  primary key (CODE)
);

CREATE TABLE POSTS
(
  POST_ID MEDIUMINT NOT NULL AUTO_INCREMENT,
  TITLE VARCHAR(130) NOT NULL,
  CONTENT VARCHAR(255) NOT NULL,
  USER_NAME VARCHAR(30) NOT NULL,
  PRIMARY KEY (POST_ID),
  FOREIGN KEY (USER_NAME) REFERENCES USER_ACCOUNT(USER_NAME)
  ON UPDATE NO ACTION ON DELETE NO ACTION
);

CREATE TABLE COMMENTS
(
  COMMENT_ID MEDIUMINT NOT NULL AUTO_INCREMENT,
  CONTENT VARCHAR(255) NOT NULL,
  USER_NAME VARCHAR(25) NOT NULL,
  POST_ID MEDIUMINT NOT NULL,
  PRIMARY KEY (COMMENT_ID),
  FOREIGN KEY (POST_ID) REFERENCES POSTS(POST_ID)
    ON UPDATE NO ACTION ON DELETE NO ACTION

);

-- Insert data: ---------------------------------------------------------------

insert into USER_ACCOUNT (USER_NAME, GENDER, PASSWORD)
values ('tom', 'M', 'tom001');

insert into USER_ACCOUNT (USER_NAME, GENDER, PASSWORD)
values ('jerry', 'M', 'jerry001');

insert into PRODUCT (CODE, NAME, PRICE)
values ('P001', 'Java Core', 100);

insert into PRODUCT (CODE, NAME, PRICE)
values ('P002', 'C# Core', 90);

INSERT INTO POSTS(POST_ID, TITLE, CONTENT, USER_NAME)
VALUES (0,'KEK','AZAZA','tom') ;

INSERT INTO COMMENTS (COMMENT_ID, CONTENT, USER_NAME, POST_ID)
VALUES (0,'Ny kakto tak','tom',1);

INSERT INTO POSTS( TITLE, CONTENT, USER_NAME)
VALUES ('KEK','OYOYO','tom') ;
